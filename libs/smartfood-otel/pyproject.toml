[project]
name = "smartfood-otel"
version = "0.1.0"
requires-python = ">=3.12,<3.13"
dependencies = ["structlog>=24.4", "prometheus-client>=0.20"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["smartfood_otel"]
