[project]
name = "smartfood-realtime"
version = "0.1.0"
requires-python = ">=3.12,<3.13"
dependencies = ["redis>=5.1"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["smartfood_realtime"]
